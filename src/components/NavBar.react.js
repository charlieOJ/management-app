const NavBar = () => {
  return (
    <aside className="fixed inset-y-0 z-40 flex max-h-screen w-16 md:w-64 flex-shrink-0 transform flex-col overflow-hidden border-r shadow-lg transition-all border-gray-700 bg-gray-900 text-slate-50">
      <h1>Title</h1>
    </aside>
  );
};

export default NavBar;
